{{!-- e.g. /data/graph/user/file.json --}}
<canvas
	id="webgl-canvas"
	class="webgl-canvas-normal"
	style="background:transparent;width:{{width}};height:{{height}};"
	oncontextmenu="return false;"
	{{#if autoplay}}
    data-autoplay="true"
    {{/if}}
	data-graph-url="{{graphSrc}}"></canvas>